import pygame
pygame.init()

import noormiextracts # if really needed here !
# import global vars (points,...) & global funcs (erase,blit,...)
# pour tous les jeux : from XXX import main

# ==== construction ====
# display main window
# buttons => each button : blit & launch it's own game function

# for each game XXX : we have a main function that launches game
# we'll need to take account of returning to main window (takccnt in XXX.main() or main()

